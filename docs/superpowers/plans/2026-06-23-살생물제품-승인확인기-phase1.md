# 살생물제품 승인 확인기 — Phase 1 구현 계획 (제품명 검색 + 전체목록 + 상세)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 환경부 chempold "승인 살생물제품" 244건을 수집해, 폰 브라우저에서 제품명 키워드로 해당 여부(✅/❌)를 즉시 확인하고 상세 정보를 가독성 있게 볼 수 있는 오프라인 단일 HTML 앱을 만든다.

**Architecture:** (1) 의존성 없는 Python(stdlib) 빌드 스크립트가 chempold에서 목록(POST 1회로 244건) + 상세 페이지를 수집·파싱해 `data/products.json` 생성 후 `app/template.html`에 데이터를 주입해 자체 완결형 `index.html`을 만든다. (2) `index.html`은 데이터가 내장된 정적 웹앱(검색/전체목록/상세 탭). GitHub Pages(소스 private, 사이트 public)로 배포한다.

**Tech Stack:** Python 3 표준 라이브러리만(`urllib`, `re`, `html`, `json`, `datetime`) · 순수 HTML/CSS/JS(프레임워크 없음) · 테스트: Python `unittest`, Node `node --test` (둘 다 무설치)

## Global Constraints

- 사이트 인코딩은 **UTF-8** (EUC-KR 아님). 응답은 `.decode("utf-8")`.
- 목록 수집: `POST https://chempold.keiti.re.kr/cpms/bcp/disc/bcp000List.do`, body `searchKey1=SC001&searchKey2=&countPerPage=1000&pageIndex=1` → 244건 1회 수신.
- 상세 수집: `GET https://chempold.keiti.re.kr/cpms/bcp/disc/BcpDisclosureView.do?bcpDiscSn=<공개번호>` (상태가 "상세보기"인 제품만; "작업중"은 sn 없음 → 상세 없음).
- 요청 헤더에 `User-Agent: Mozilla/5.0` 포함. 상세 다건 요청 사이 `time.sleep(0.3)`로 예의.
- **외부 의존성(pip/npm) 금지** — stdlib/내장 러너만 사용.
- 검색 정규화는 Python·JS가 동일해야 함: **공백 전부 제거 + 소문자화** (`normalize`).
- "해당 안됨"은 "이 목록에 없음"이지 법적 무허가 판정이 아님 — 화면에 단서 문구 표시.
- 결과 화면은 큰 ✅/❌ 배지로 한눈에 보이게. 작업중 제품도 ✅(해당됨)로 취급, 상세 버튼만 비활성.
- 최종 배포 산출물 파일명은 GitHub Pages 기본 진입점인 `index.html`.

---

## 파일 구조

- `build/parse.py` — 순수 파싱·정규화 함수 (네트워크 없음). 책임: HTML→구조화 데이터.
- `build/fetch.py` — chempold 네트워크 수집 (urllib). 책임: 목록/상세 HTML 가져오기.
- `build/build.py` — 오케스트레이션: 수집→조립→`products.json`→`index.html` 렌더. CLI 진입점.
- `app/template.html` — 앱 셸(UI/CSS). 데이터·검색JS 주입 마커 포함.
- `app/search.js` — 순수 검색 함수(`normalize`, `matchProducts`). 브라우저·Node 공용.
- `data/products.json` — 생성물(빌드 산출).
- `index.html` — 생성물(최종 배포 앱, 자체 완결형).
- `tests/fixtures/list_sample.html` — "홈매트" 검색 결과(5행: 상세보기 3 + 작업중 2) 실데이터 픽스처.
- `tests/fixtures/detail_sample.html` — 공개번호 2025-182 상세 픽스처.
- `tests/test_parse_list.py`, `tests/test_parse_detail.py`, `tests/test_build.py` — Python 단위테스트.
- `tests/search.test.mjs` — `app/search.js` Node 테스트.
- `README.md` — 빌드 실행법 + GitHub Pages 배포법.
- `.gitignore` — `__pycache__/` 등.

> **Phase 2(바코드 스캔·메모)는 별도 계획 문서로 분리한다.** 본 계획은 Phase 1만 다루며, 그 자체로 동작·배포 가능한 산출물을 만든다.

---

### Task 1: 프로젝트 골격 + 실데이터 테스트 픽스처

**Files:**
- Create: `.gitignore`, `README.md`(골격), `tests/fixtures/list_sample.html`, `tests/fixtures/detail_sample.html`

**Interfaces:**
- Consumes: (없음)
- Produces: 픽스처 2종(후속 파싱 테스트의 입력). `list_sample.html`은 "홈매트" 검색 5행, `detail_sample.html`은 2025-182 상세.

- [ ] **Step 1: 디렉터리·.gitignore 생성**

`.gitignore`:
```
__pycache__/
*.pyc
.DS_Store
```

- [ ] **Step 2: 픽스처 생성 스크립트 실행 (실데이터 저장)**

Run (프로젝트 루트에서):
```bash
python - <<'PY'
import urllib.request, urllib.parse, os
os.makedirs("tests/fixtures", exist_ok=True)
BASE="https://chempold.keiti.re.kr/cpms/bcp/disc/"
UA={"User-Agent":"Mozilla/5.0"}
# 목록: '홈매트' 검색 (제품명=SC002) → 5행
body=urllib.parse.urlencode({"pageIndex":"1","searchKey1":"SC002","searchKey2":"홈매트","countPerPage":"100"}).encode()
req=urllib.request.Request(BASE+"bcp000List.do", data=body, headers=UA)
open("tests/fixtures/list_sample.html","w",encoding="utf-8").write(urllib.request.urlopen(req,timeout=60).read().decode("utf-8"))
# 상세: 2025-182
req2=urllib.request.Request(BASE+"BcpDisclosureView.do?"+urllib.parse.urlencode({"bcpDiscSn":"2025-182"}), headers=UA)
open("tests/fixtures/detail_sample.html","w",encoding="utf-8").write(urllib.request.urlopen(req2,timeout=60).read().decode("utf-8"))
print("fixtures saved")
PY
```
Expected: `fixtures saved` 출력, 두 파일 생성.

- [ ] **Step 3: 픽스처 내용 검증**

Run:
```bash
python - <<'PY'
a=open("tests/fixtures/list_sample.html",encoding="utf-8").read()
b=open("tests/fixtures/detail_sample.html",encoding="utf-8").read()
assert "홈매트 리퀴드 에스" in a, "list fixture missing product"
assert "작업중" in a, "list fixture missing 작업중 row"
assert "추가제품명" in b and "홈키파" in b, "detail fixture missing 추가제품명/홈키파"
print("fixtures OK")
PY
```
Expected: `fixtures OK`.

- [ ] **Step 4: README 골격 작성**

`README.md`:
```markdown
# 살생물제품 승인 확인기

환경부 chempold "승인 살생물제품" 목록을 폰에서 검색하는 오프라인 웹앱.

## 빌드 (데이터 수집 → index.html 생성)
필요: Python 3 (추가 설치 불필요)
```
python build/build.py
```
생성물: `data/products.json`, `index.html`

## 테스트
```
python -m unittest discover -s tests -p "test_*.py" -v
node --test tests/
```

## 배포 (작성 예정: Task 7)
```

- [ ] **Step 5: 커밋**

```bash
git init
git add .gitignore README.md tests/fixtures/
git commit -m "chore: project skeleton and live data fixtures"
```

---

### Task 2: 목록 파서 + 정규화 (`build/parse.py`)

**Files:**
- Create: `build/parse.py`
- Test: `tests/test_parse_list.py`

**Interfaces:**
- Consumes: `tests/fixtures/list_sample.html`
- Produces:
  - `normalize(s: str) -> str` — 공백 제거 + 소문자.
  - `parse_list(html: str) -> list[dict]` — 각 dict 키: `순번,공개번호,제품명,물질명,유형,사용자,유효기간,상태,sn`(작업중이면 `sn=None`).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_parse_list.py`:
```python
import os, sys, unittest
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "build"))
from parse import parse_list, normalize

FIX = os.path.join(os.path.dirname(__file__), "fixtures", "list_sample.html")

class TestParseList(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        with open(FIX, encoding="utf-8") as f:
            cls.products = parse_list(f.read())

    def test_count_is_five(self):
        self.assertEqual(len(self.products), 5)

    def test_first_product_fields(self):
        p = self.products[0]
        self.assertEqual(p["제품명"], "홈매트 리퀴드 에스")
        self.assertEqual(p["공개번호"], "2025-182")
        self.assertEqual(p["상태"], "상세보기")
        self.assertEqual(p["sn"], "2025-182")
        self.assertIn("Prallethrin", p["물질명"])

    def test_jakeopjung_has_no_sn(self):
        jak = [p for p in self.products if p["상태"] == "작업중"]
        self.assertEqual(len(jak), 2)
        self.assertTrue(all(p["sn"] is None for p in jak))

    def test_normalize(self):
        self.assertEqual(normalize("홈매트 리퀴드 ES"), "홈매트리퀴드es")
        self.assertEqual(normalize("  A  B  "), "ab")

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m unittest tests.test_parse_list -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'parse'` 또는 import 에러.

- [ ] **Step 3: 최소 구현 작성**

`build/parse.py`:
```python
"""chempold HTML 파싱 및 정규화 (순수 함수, 네트워크 없음)."""
import re
import html as _html

_TAG = re.compile(r"<[^>]+>")
_WS = re.compile(r"\s+")
_ROW = re.compile(r"<tr>(.*?)</tr>", re.S)
_CELL = re.compile(r"<td[^>]*>(.*?)</td>", re.S)
_VIEW = re.compile(r"view\('([^']+)'\)")
_PAIR = re.compile(r"<th[^>]*>(.*?)</th>\s*<td[^>]*>(.*?)</td>", re.S)


def normalize(s):
    """공백 전부 제거 + 소문자화. JS normalize와 동일 규칙."""
    return _WS.sub("", s or "").lower()


def _cell_text(fragment):
    # 태그만 제거(공백 삽입 안 함) → 물질명의 줄바꿈/CAS 구분 유지
    return _html.unescape(_TAG.sub("", fragment)).replace("\xa0", " ").strip()


def parse_list(html):
    """목록 HTML → 제품 레코드 리스트."""
    products = []
    for row in _ROW.findall(html):
        cells = [_cell_text(c) for c in _CELL.findall(row)]
        if len(cells) < 8:
            continue
        m = _VIEW.search(row)
        products.append({
            "순번": cells[0],
            "공개번호": cells[1],
            "제품명": cells[2],
            "물질명": cells[3],
            "유형": cells[4],
            "사용자": cells[5],
            "유효기간": cells[6],
            "상태": cells[7],
            "sn": m.group(1) if m else None,
        })
    return products
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m unittest tests.test_parse_list -v`
Expected: PASS (4 tests OK).

- [ ] **Step 5: 커밋**

```bash
git add build/parse.py tests/test_parse_list.py
git commit -m "feat: list HTML parser and normalize()"
```

---

### Task 3: 상세 파서 (`build/parse.py`에 추가)

**Files:**
- Modify: `build/parse.py`
- Test: `tests/test_parse_detail.py`

**Interfaces:**
- Consumes: `tests/fixtures/detail_sample.html`
- Produces:
  - `parse_detail(html: str) -> list[list[str]]` — `[[label, value], ...]` 순서 보존.
  - `extra_names(pairs: list[list[str]]) -> str` — "추가제품명" 값(없으면 `""`).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_parse_detail.py`:
```python
import os, sys, unittest
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "build"))
from parse import parse_detail, extra_names

FIX = os.path.join(os.path.dirname(__file__), "fixtures", "detail_sample.html")

class TestParseDetail(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        with open(FIX, encoding="utf-8") as f:
            cls.pairs = parse_detail(f.read())

    def test_has_many_pairs(self):
        self.assertGreater(len(self.pairs), 50)

    def test_pairs_are_label_value(self):
        d = dict(self.pairs)
        self.assertEqual(d.get("정보공개번호"), "2025-182")
        self.assertEqual(d.get("제품명"), "홈매트 리퀴드 에스")
        self.assertEqual(d.get("살생물제품유형"), "살충제")

    def test_extra_names_contains_homekipa(self):
        self.assertIn("홈키파", extra_names(self.pairs))

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m unittest tests.test_parse_detail -v`
Expected: FAIL — `ImportError: cannot import name 'parse_detail'`.

- [ ] **Step 3: 최소 구현 추가**

`build/parse.py` 끝에 추가:
```python
def _detail_text(fragment):
    return _WS.sub(" ", _html.unescape(_TAG.sub(" ", fragment))).strip()


def parse_detail(html):
    """상세 HTML → [[label, value], ...] (순서 보존)."""
    pairs = []
    for th, td in _PAIR.findall(html):
        label = _detail_text(th)
        value = _detail_text(td)
        if label:
            pairs.append([label, value])
    return pairs


def extra_names(pairs):
    """'추가제품명' 값 반환(없으면 빈 문자열)."""
    for label, value in pairs:
        if "추가제품명" in label:
            return value
    return ""
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m unittest tests.test_parse_detail -v`
Expected: PASS (3 tests OK).

- [ ] **Step 5: 커밋**

```bash
git add build/parse.py tests/test_parse_detail.py
git commit -m "feat: detail page parser and 추가제품명 extraction"
```

---

### Task 4: 네트워크 수집기 (`build/fetch.py`)

**Files:**
- Create: `build/fetch.py`
- Test: `tests/test_build.py` (요청 구성 단위테스트 부분)

**Interfaces:**
- Consumes: (네트워크) chempold 엔드포인트
- Produces:
  - `list_request() -> (url:str, data:bytes, headers:dict)` — 목록 POST 요청 구성(순수, 테스트 가능).
  - `detail_url(sn:str) -> str` — 상세 GET URL(순수).
  - `fetch_list_html(count:int=1000) -> str` — 목록 HTML(네트워크).
  - `fetch_detail_html(sn:str) -> str` — 상세 HTML(네트워크).

- [ ] **Step 1: 실패하는 테스트 작성 (요청 구성만 단위테스트)**

`tests/test_build.py` (신규, fetch 부분만 우선):
```python
import os, sys, unittest
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "build"))
import fetch

class TestFetchRequest(unittest.TestCase):
    def test_list_request_params(self):
        url, data, headers = fetch.list_request(1000)
        self.assertIn("bcp000List.do", url)
        body = data.decode()
        self.assertIn("searchKey1=SC001", body)
        self.assertIn("countPerPage=1000", body)
        self.assertIn("Mozilla", headers.get("User-Agent", ""))

    def test_detail_url(self):
        self.assertEqual(
            fetch.detail_url("2025-182"),
            "https://chempold.keiti.re.kr/cpms/bcp/disc/BcpDisclosureView.do?bcpDiscSn=2025-182",
        )

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m unittest tests.test_build -v`
Expected: FAIL — `No module named 'fetch'`.

- [ ] **Step 3: 최소 구현 작성**

`build/fetch.py`:
```python
"""chempold 네트워크 수집 (표준 라이브러리 urllib만 사용)."""
import time
import urllib.request
import urllib.parse

BASE = "https://chempold.keiti.re.kr/cpms/bcp/disc/"
_HEADERS = {"User-Agent": "Mozilla/5.0"}


def list_request(count=1000):
    url = BASE + "bcp000List.do"
    data = urllib.parse.urlencode({
        "pageIndex": "1",
        "searchKey1": "SC001",
        "searchKey2": "",
        "countPerPage": str(count),
    }).encode("utf-8")
    return url, data, dict(_HEADERS)


def detail_url(sn):
    return BASE + "BcpDisclosureView.do?" + urllib.parse.urlencode({"bcpDiscSn": sn})


def _get(url, data=None, headers=None):
    req = urllib.request.Request(url, data=data, headers=headers or dict(_HEADERS))
    with urllib.request.urlopen(req, timeout=60) as resp:
        return resp.read().decode("utf-8")


def fetch_list_html(count=1000):
    url, data, headers = list_request(count)
    return _get(url, data=data, headers=headers)


def fetch_detail_html(sn):
    return _get(detail_url(sn))
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m unittest tests.test_build -v`
Expected: PASS (2 tests OK).

- [ ] **Step 5: 네트워크 스모크 확인 (수동, 1회)**

Run:
```bash
python -c "import sys; sys.path.insert(0,'build'); import fetch; h=fetch.fetch_list_html(10); print('len',len(h),'ok' if '총게시물' in h else 'FAIL')"
```
Expected: `len <수천+> ok`. (실패 시 SSL/네트워크 문제 — README 트러블슈팅에 기록)

- [ ] **Step 6: 커밋**

```bash
git add build/fetch.py tests/test_build.py
git commit -m "feat: chempold network fetcher (urllib)"
```

---

### Task 5: 조립 + 렌더 (`build/build.py`) — 데이터/검색JS 주입

**Files:**
- Create: `build/build.py`
- Modify: `tests/test_build.py` (assemble/render 테스트 추가)

**Interfaces:**
- Consumes: `parse.parse_list/parse_detail/extra_names/normalize`, `fetch.*`
- Produces:
  - `build_search_text(product:dict, detail_pairs:list) -> str` — 정규화된 검색 색인(제품명+물질명+추가제품명).
  - `assemble(list_records:list, details_by_sn:dict) -> list[dict]` — 각 제품에 `상세`(pairs)와 `검색텍스트` 추가.
  - `render_html(template:str, search_js:str, db:dict) -> str` — 마커 치환으로 자체완결 HTML 생성.
  - `main(out_root:str=".", 기준일:str|None=None) -> None` — 전체 빌드(수집→조립→파일쓰기).
  - 치환 마커: 템플릿 내 `__DB_JSON__`(데이터), `/*__SEARCH_JS__*/`(검색JS).

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_build.py`에 추가:
```python
import build as builder  # build/build.py

class TestAssembleRender(unittest.TestCase):
    def test_search_text_includes_extra_names(self):
        product = {"제품명": "홈매트 리퀴드 에스", "물질명": "Prallethrin (CAS No. 23031-36-9)"}
        pairs = [["추가제품명", "홈키파 홈매트, 홈키파 리퀴드"]]
        st = builder.build_search_text(product, pairs)
        self.assertIn("홈키파", st)
        self.assertIn("홈매트리퀴드에스", st)
        self.assertEqual(st, st.lower())
        self.assertNotIn(" ", st)

    def test_assemble_attaches_detail_and_searchtext(self):
        records = [{"제품명": "홈매트 리퀴드 에스", "물질명": "P", "sn": "2025-182"},
                   {"제품명": "작업중품", "물질명": "Q", "sn": None}]
        details = {"2025-182": [["추가제품명", "홈키파"]]}
        out = builder.assemble(records, details)
        self.assertEqual(out[0]["상세"], [["추가제품명", "홈키파"]])
        self.assertIn("홈키파", out[0]["검색텍스트"])
        self.assertEqual(out[1]["상세"], [])

    def test_render_injects_data_and_js(self):
        template = 'A /*__SEARCH_JS__*/ B var DB = __DB_JSON__; C'
        search_js = "function normalize(s){return s;}\nexport { normalize };\n"
        db = {"기준일": "2026-06-23", "제품": [{"제품명": "홈매트"}]}
        html = builder.render_html(template, search_js, db)
        self.assertIn("function normalize", html)
        self.assertNotIn("export {", html)          # export 줄 제거됨
        self.assertIn('"2026-06-23"', html)
        self.assertIn("홈매트", html)
        self.assertNotIn("__DB_JSON__", html)        # 토큰 모두 치환
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m unittest tests.test_build -v`
Expected: FAIL — `No module named 'build'`.

- [ ] **Step 3: 최소 구현 작성**

`build/build.py`:
```python
"""빌드 오케스트레이션: 수집 → 조립 → products.json + index.html 생성."""
import os
import re
import sys
import json
import time
import datetime

sys.path.insert(0, os.path.dirname(__file__))
import fetch
from parse import parse_list, parse_detail, extra_names, normalize

_EXPORT_LINE = re.compile(r"(?m)^\s*export\s*\{[^}]*\};?\s*$")


def build_search_text(product, detail_pairs):
    parts = [product.get("제품명", ""), product.get("물질명", ""), extra_names(detail_pairs)]
    return normalize(" ".join(p for p in parts if p))


def assemble(list_records, details_by_sn):
    out = []
    for r in list_records:
        item = dict(r)
        detail = details_by_sn.get(r.get("sn")) or []
        item["상세"] = detail
        item["검색텍스트"] = build_search_text(r, detail)
        out.append(item)
    return out


def render_html(template, search_js, db):
    js = _EXPORT_LINE.sub("", search_js)          # 브라우저용: export 줄 제거
    out = template.replace("/*__SEARCH_JS__*/", js)
    out = out.replace("__DB_JSON__", json.dumps(db, ensure_ascii=False))
    return out


def main(out_root=".", 기준일=None):
    기준일 = 기준일 or datetime.date.today().isoformat()
    print("목록 수집 중...")
    records = parse_list(fetch.fetch_list_html(1000))
    print(f"  목록 {len(records)}건")
    details = {}
    targets = [r for r in records if r.get("sn")]
    for i, r in enumerate(targets, 1):
        details[r["sn"]] = parse_detail(fetch.fetch_detail_html(r["sn"]))
        if i % 20 == 0:
            print(f"  상세 {i}/{len(targets)}")
        time.sleep(0.3)
    products = assemble(records, details)
    db = {
        "기준일": 기준일,
        "총건수": len(products),
        "출처": fetch.BASE + "bcp000List.do",
        "제품": products,
    }
    os.makedirs(os.path.join(out_root, "data"), exist_ok=True)
    with open(os.path.join(out_root, "data", "products.json"), "w", encoding="utf-8") as f:
        json.dump(db, f, ensure_ascii=False, indent=1)
    with open(os.path.join(out_root, "app", "template.html"), encoding="utf-8") as f:
        template = f.read()
    with open(os.path.join(out_root, "app", "search.js"), encoding="utf-8") as f:
        search_js = f.read()
    html = render_html(template, search_js, db)
    with open(os.path.join(out_root, "index.html"), "w", encoding="utf-8") as f:
        f.write(html)
    print(f"완료: index.html ({len(products)}건, 기준일 {기준일})")


if __name__ == "__main__":
    main()
```

> 참고: `main()`은 Task 6에서 `app/template.html`·`app/search.js`가 생긴 뒤 실제 실행한다. 본 Task에서는 순수 함수(`build_search_text/assemble/render_html`) 테스트만 통과시키면 된다.

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m unittest tests.test_build -v`
Expected: PASS (요청 구성 2 + 조립/렌더 3 = 5 tests OK).

- [ ] **Step 5: 커밋**

```bash
git add build/build.py tests/test_build.py
git commit -m "feat: assemble + render with data/search-js injection"
```

---

### Task 6: 웹앱 (`app/search.js` + `app/template.html`) 및 첫 빌드

**Files:**
- Create: `app/search.js`, `app/template.html`
- Test: `tests/search.test.mjs`

**Interfaces:**
- Consumes: 빌드 시 주입되는 `DB`(= products.json 구조), `normalize/matchProducts`.
- Produces: `normalize(s)`, `matchProducts(products, query)` (브라우저 전역 + ESM export).

- [ ] **Step 1: 검색 JS 실패 테스트 작성**

`tests/search.test.mjs`:
```javascript
import { test } from "node:test";
import assert from "node:assert/strict";
import { normalize, matchProducts } from "../app/search.js";

const DB = [
  { 제품명: "홈매트 리퀴드 에스", 검색텍스트: "홈매트리퀴드에스홈키파홈매트prallethrin" },
  { 제품명: "베스핀쓰리", 검색텍스트: "베스핀쓰리clorofene" },
];

test("normalize removes spaces and lowercases", () => {
  assert.equal(normalize("홈매트 리퀴드 ES"), "홈매트리퀴드es");
});

test("matchProducts finds by partial keyword (홈키파 via 추가제품명)", () => {
  const r = matchProducts(DB, "홈키파");
  assert.equal(r.length, 1);
  assert.equal(r[0].제품명, "홈매트 리퀴드 에스");
});

test("matchProducts ignores spacing", () => {
  assert.equal(matchProducts(DB, "홈 매 트").length, 1);
});

test("empty query returns nothing", () => {
  assert.equal(matchProducts(DB, "   ").length, 0);
});

test("no match returns empty", () => {
  assert.equal(matchProducts(DB, "타이레놀").length, 0);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test tests/`
Expected: FAIL — `Cannot find module '../app/search.js'`.

- [ ] **Step 3: `app/search.js` 구현**

`app/search.js`:
```javascript
// 순수 검색 함수 (브라우저·Node 공용). Python parse.normalize와 동일 규칙.
function normalize(s) {
  return (s || "").replace(/\s+/g, "").toLowerCase();
}

function matchProducts(products, query) {
  const q = normalize(query);
  if (!q) return [];
  return products.filter((p) => (p.검색텍스트 || "").includes(q));
}

export { normalize, matchProducts };
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test tests/`
Expected: PASS (5 tests).

- [ ] **Step 5: `app/template.html` 작성 (앱 셸)**

`app/template.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>살생물제품 승인 확인</title>
<style>
  :root { --green:#1a9e4b; --red:#d63a3a; --line:#e3e6ea; --muted:#6b7280; }
  * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
  body { margin:0; font-family:-apple-system,"Apple SD Gothic Neo","Malgun Gothic",sans-serif;
         color:#1f2430; background:#f5f6f8; padding-bottom:72px; }
  header { background:#fff; padding:14px 16px; border-bottom:1px solid var(--line); }
  header h1 { font-size:18px; margin:0; }
  header .meta { font-size:12px; color:var(--muted); margin-top:4px; }
  .wrap { padding:14px 16px; }
  .combo { position:relative; }
  .combo input { width:100%; font-size:17px; padding:13px 14px; border:1px solid var(--line);
                 border-radius:12px; outline:none; }
  .combo input:focus { border-color:var(--green); }
  .suggest { list-style:none; margin:6px 0 0; padding:0; background:#fff; border:1px solid var(--line);
             border-radius:12px; overflow:hidden; max-height:60vh; overflow-y:auto; }
  .suggest li { padding:13px 14px; border-bottom:1px solid var(--line); display:flex;
                justify-content:space-between; gap:10px; align-items:center; cursor:pointer; }
  .suggest li:last-child { border-bottom:0; }
  .suggest li:active { background:#f0f7f2; }
  .suggest .snm { font-size:15px; }
  .suggest .sty { font-size:12px; color:var(--muted); flex:0 0 auto; }
  .suggest .nomatch { color:var(--muted); justify-content:center; cursor:default; }
  .suggest .noneitem { background:#fff7f7; color:var(--red); font-weight:700; font-size:14px; }
  .suggest .noneitem b { color:#1f2430; }
  .hint { font-size:12px; color:var(--muted); margin:8px 2px 0; }
  .summary { font-size:14px; font-weight:700; margin:16px 2px 10px; }
  .card { background:#fff; border:1px solid var(--line); border-radius:14px; padding:14px;
          margin-bottom:10px; }
  .card .name { font-size:16px; font-weight:700; }
  .row { font-size:13px; color:#333; margin-top:6px; line-height:1.45; }
  .row b { color:var(--muted); font-weight:600; }
  .badges { display:flex; gap:6px; align-items:center; margin-bottom:8px; flex-wrap:wrap; }
  .badge { font-size:12px; font-weight:700; padding:4px 9px; border-radius:999px; }
  .badge.ok { background:#e7f6ec; color:var(--green); }
  .badge.no { background:#fde8e8; color:var(--red); }
  .badge.wip { background:#fff3e0; color:#b26a00; }
  .detailbtn { margin-top:10px; font-size:14px; padding:9px 12px; border:1px solid var(--green);
               color:var(--green); background:#fff; border-radius:10px; font-weight:700; width:100%; }
  .detailbtn[disabled] { border-color:var(--line); color:#aaa; }
  .empty { text-align:center; color:var(--muted); padding:40px 10px; }
  .noresult { background:#fff; border:1px solid var(--line); border-radius:14px; padding:20px; text-align:center; }
  .noresult .big { font-size:18px; font-weight:800; color:var(--red); }
  .noresult p { font-size:13px; color:var(--muted); margin:10px 0 0; line-height:1.5; }
  .chips { display:flex; gap:6px; flex-wrap:wrap; margin-bottom:12px; }
  .chip { font-size:13px; padding:7px 12px; border:1px solid var(--line); border-radius:999px;
          background:#fff; }
  .chip.active { background:var(--green); color:#fff; border-color:var(--green); }
  nav { position:fixed; left:0; right:0; bottom:0; display:flex; background:#fff;
        border-top:1px solid var(--line); }
  nav button { flex:1; padding:12px 0 14px; border:0; background:none; font-size:13px; color:var(--muted); }
  nav button.active { color:var(--green); font-weight:800; }
  #detail { position:fixed; inset:0; background:#f5f6f8; z-index:20; display:none; flex-direction:column; }
  #detail.open { display:flex; }
  #detail .top { background:#fff; padding:12px 14px; border-bottom:1px solid var(--line);
                 display:flex; align-items:center; gap:10px; }
  #detail .top button { border:0; background:none; font-size:16px; color:var(--green); font-weight:700; }
  #detail .body { overflow:auto; padding:14px 16px; }
  dl { margin:0; }
  dl .item { border-bottom:1px solid var(--line); padding:9px 0; }
  dl dt { font-size:12px; color:var(--muted); font-weight:600; }
  dl dd { margin:3px 0 0; font-size:14px; line-height:1.5; white-space:pre-wrap; }
  .srclink { display:inline-block; margin:16px 0 8px; font-size:13px; color:var(--green); }
  .hidden { display:none !important; }
</style>
</head>
<body>
<header>
  <h1>살생물제품 승인 확인</h1>
  <div class="meta" id="meta"></div>
</header>

<main>
  <section id="view-search" class="wrap">
    <div class="combo">
      <input id="q" type="search" inputmode="search" placeholder="제품명 입력 (예: 홈키파, 에프킬라)" autocomplete="off">
      <ul id="suggest" class="suggest hidden"></ul>
    </div>
    <div class="hint">제품명을 입력하면 후보가 아래에 뜹니다(공백·대소문자 무시). 맞는 게 없으면 맨 아래 "없음"을 선택하세요.</div>
    <div id="result"><div class="empty">제품명을 입력해 조회하세요.</div></div>
  </section>

  <section id="view-list" class="wrap hidden">
    <div class="chips" id="chips"></div>
    <div id="listResult"></div>
  </section>
</main>

<nav>
  <button id="tab-search" class="active">🔍 검색</button>
  <button id="tab-list">📋 전체 목록</button>
</nav>

<div id="detail">
  <div class="top"><button id="detailBack">← 뒤로</button><strong id="detailTitle"></strong></div>
  <div class="body" id="detailBody"></div>
</div>

<script>
/*__SEARCH_JS__*/
</script>
<script>
const DB = __DB_JSON__;
const $ = (s) => document.querySelector(s);
const esc = (s) => (s || "").replace(/[&<>]/g, (c) => ({ "&":"&amp;","<":"&lt;",">":"&gt;" }[c]));
const SRC = "https://chempold.keiti.re.kr/cpms/bcp/disc/BcpDisclosureView.do?bcpDiscSn=";

$("#meta").textContent = `기준일 ${DB.기준일} · 총 ${DB.총건수}건 (출처: 환경부 chempold)`;

function statusBadge(p) {
  return p.상태 === "작업중"
    ? '<span class="badge wip">상세 준비중</span>'
    : '<span class="badge ok">상세보기 가능</span>';
}

function cardHTML(p) {
  const detailBtn = p.상태 === "작업중" || !p.sn
    ? '<button class="detailbtn" disabled>상세 준비중</button>'
    : `<button class="detailbtn" data-no="${esc(p.공개번호)}">상세보기</button>`;
  return `<div class="card">
    <div class="badges"><span class="badge ok">✅ 해당됨 (승인 살생물제품)</span>${statusBadge(p)}</div>
    <div class="name">${esc(p.제품명)}</div>
    <div class="row"><b>유형</b> ${esc(p.유형)}</div>
    <div class="row"><b>살생물물질</b> ${esc(p.물질명)}</div>
    <div class="row"><b>사용자</b> ${esc(p.사용자)}</div>
    <div class="row"><b>유효기간</b> ${esc(p.유효기간)}</div>
    <div class="row"><b>정보공개번호</b> ${esc(p.공개번호)}</div>
    ${detailBtn}
  </div>`;
}

const MAX_SUGGEST = 30;

function renderSuggest(query) {
  const ul = $("#suggest");
  const q = query.trim();
  if (!q) { ul.classList.add("hidden"); ul.innerHTML = ""; return; }
  const matches = matchProducts(DB.제품, q).slice(0, MAX_SUGGEST);
  let rows = matches.map((p) =>
    `<li data-no="${esc(p.공개번호)}"><span class="snm">${esc(p.제품명)}</span>` +
    `<span class="sty">${esc(p.유형)}</span></li>`
  ).join("");
  if (matches.length === 0) rows = '<li class="nomatch">일치하는 제품이 없습니다</li>';
  rows += `<li class="noneitem" data-none="1">✕ 없음 — "<b>${esc(q)}</b>" 목록에 없음(해당없음)</li>`;
  ul.innerHTML = rows;
  ul.classList.remove("hidden");
}

function selectProduct(no) {
  const p = DB.제품.find((x) => x.공개번호 === no);
  if (!p) return;
  $("#q").value = p.제품명;
  $("#suggest").classList.add("hidden");
  $("#result").innerHTML = `<div class="summary">✅ 승인 살생물제품에 해당</div>` + cardHTML(p);
}

function selectNone(query) {
  const q = query.trim();
  if (!q) return;
  if (!confirm(`"${q}"(으)로 검색했는데 목록에 없습니다.\n해당없음으로 처리할까요?`)) return;
  $("#suggest").classList.add("hidden");
  $("#result").innerHTML = `<div class="noresult">
    <div class="big">❌ 해당 안됨</div>
    <p>"<b>${esc(q)}</b>"은(는) 이 승인 살생물제품 목록(${DB.총건수}건)에 없습니다.<br>
    띄어쓰기·오타를 확인하거나 더 짧은 키워드로 다시 검색해 보세요.<br>
    <small>※ "해당 안됨"은 이 목록에 없다는 뜻이며, 불법·무허가 판정이 아닙니다.</small></p>
  </div>`;
}

function openDetail(no) {
  const p = DB.제품.find((x) => x.공개번호 === no);
  if (!p) return;
  $("#detailTitle").textContent = p.제품명;
  const items = (p.상세 || []).map(
    ([k, v]) => `<div class="item"><dt>${esc(k)}</dt><dd>${esc(v)}</dd></div>`
  ).join("");
  const link = p.sn
    ? `<a class="srclink" href="${SRC}${esc(p.sn)}" target="_blank" rel="noopener">공식 원문 보기 ↗</a>`
    : "";
  $("#detailBody").innerHTML = items
    ? `<dl>${items}</dl>${link}`
    : '<div class="empty">상세 정보 준비중입니다.</div>';
  $("#detail").classList.add("open");
}

function renderList(type) {
  const items = type === "전체" ? DB.제품 : DB.제품.filter((p) => (p.유형 || "").includes(type));
  $("#listResult").innerHTML =
    `<div class="summary">${items.length}건</div>` + items.map(cardHTML).join("");
}

// 유형 칩 구성
const types = ["전체", ...Array.from(new Set(DB.제품.map((p) => p.유형).filter(Boolean)))];
$("#chips").innerHTML = types
  .map((t, i) => `<button class="chip${i === 0 ? " active" : ""}" data-type="${esc(t)}">${esc(t)}</button>`)
  .join("");

// 이벤트
$("#q").addEventListener("input", () => renderSuggest($("#q").value));
$("#q").addEventListener("focus", () => renderSuggest($("#q").value));
$("#suggest").addEventListener("click", (e) => {
  if (e.target.closest("[data-none]")) { selectNone($("#q").value); return; }
  const item = e.target.closest("li[data-no]");
  if (item) selectProduct(item.dataset.no);
});
$("#detailBack").addEventListener("click", () => $("#detail").classList.remove("open"));
document.addEventListener("click", (e) => {
  const b = e.target.closest(".detailbtn[data-no]");
  if (b) openDetail(b.dataset.no);
  const c = e.target.closest(".chip");
  if (c) {
    document.querySelectorAll(".chip").forEach((x) => x.classList.toggle("active", x === c));
    renderList(c.dataset.type);
  }
});
function switchTab(which) {
  $("#tab-search").classList.toggle("active", which === "search");
  $("#tab-list").classList.toggle("active", which === "list");
  $("#view-search").classList.toggle("hidden", which !== "search");
  $("#view-list").classList.toggle("hidden", which !== "list");
  if (which === "list" && !$("#listResult").innerHTML) renderList("전체");
}
$("#tab-search").addEventListener("click", () => switchTab("search"));
$("#tab-list").addEventListener("click", () => switchTab("list"));
</script>
</body>
</html>
```

- [ ] **Step 6: 첫 실제 빌드 실행 (네트워크, 약 1~2분)**

Run (프로젝트 루트):
```bash
python build/build.py
```
Expected: `완료: index.html (244건, 기준일 ...)` 출력. `index.html`, `data/products.json` 생성.

- [ ] **Step 7: 빌드 산출물 스모크 검증**

Run:
```bash
python - <<'PY'
import json
db=json.load(open("data/products.json",encoding="utf-8"))
assert db["총건수"]==len(db["제품"]) and db["총건수"]>=244, db["총건수"]
hk=[p for p in db["제품"] if "홈키파" in p["검색텍스트"]]
assert hk, "홈키파 색인 누락"
h=open("index.html",encoding="utf-8").read()
assert "function normalize" in h and "__DB_JSON__" not in h and "홈매트" in h
print("build OK:", db["총건수"], "건, 홈키파매칭", len(hk))
PY
```
Expected: `build OK: 244 건, 홈키파매칭 N`.

- [ ] **Step 8: 브라우저 수동 확인 (가독성·동작)**

`index.html`을 PC 브라우저에서 열어 확인(체크리스트):
  - "홈키파" 입력 → 드롭다운에 홈매트 계열 후보가 뜸 → 후보 선택 시 ✅ 해당됨 카드
  - "홈매트" 입력 → 드롭다운 5건 후보(상세보기/작업중) + 맨 아래 "없음" 항목
  - 후보 선택 → ✅ 카드, 상세보기 클릭 → 상세 항목 라벨/값으로 가독성 있게 표시 + "공식 원문 보기" 링크
  - "작업중" 제품 카드 → 상세 버튼이 "상세 준비중"으로 비활성
  - "타이레놀" 입력 → "일치하는 제품이 없습니다" + "없음" 선택 → 되묻기 후 ❌ 해당 안됨(입력어 echo + 단서 문구)
  - [전체 목록] 탭 → 244건, 유형 칩(살충제/살균제 등) 필터 동작
  - 모바일 폭(개발자도구 모바일뷰)에서 드롭다운/레이아웃 정상

- [ ] **Step 9: 커밋**

```bash
git add app/search.js app/template.html tests/search.test.mjs index.html data/products.json
git commit -m "feat: mobile search/list/detail web app + first data build"
```

---

### Task 7: 배포 문서화 (GitHub Pages: 소스 private + 사이트 public)

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: 빌드 산출 `index.html`
- Produces: 재현 가능한 배포·갱신 절차 문서

- [ ] **Step 1: README 배포 섹션 작성**

`README.md`의 "배포" 부분을 다음으로 교체:
```markdown
## 배포 (GitHub Pages — 소스 private, 사이트 public)

1. GitHub에서 **private** 저장소 생성(예: `bio-check`).
2. 이 폴더를 그 저장소로 push:
   ```
   git branch -M main
   git remote add origin https://github.com/<아이디>/bio-check.git
   git push -u origin main
   ```
3. 저장소 → Settings → Pages → Build and deployment:
   - Source: **Deploy from a branch**
   - Branch: **main** / 폴더 **/ (root)** → Save
4. 1~2분 뒤 `https://<아이디>.github.io/bio-check/` 접속 → 폰에서 이 링크 사용.
   (저장소는 private이라 소스/스크립트는 비공개, 배포된 페이지만 공개)

## 데이터 갱신
```
python build/build.py
git add index.html data/products.json
git commit -m "data: refresh"
git push
```
push 즉시 모든 사용자에게 반영됨.

## 트러블슈팅
- 빌드 시 SSL/네트워크 오류: 회사망/방화벽 확인. 재시도. (사이트는 UTF-8, POST `countPerPage=1000`로 전 건 수신)
- "작업중" 제품 47건은 상세 페이지가 없어 리스트명으로만 검색됨(정상).
```

- [ ] **Step 2: 문서 정합성 확인**

Run:
```bash
python - <<'PY'
r=open("README.md",encoding="utf-8").read()
assert "Deploy from a branch" in r and "python build/build.py" in r
print("README OK")
PY
```
Expected: `README OK`.

- [ ] **Step 3: 커밋**

```bash
git add README.md
git commit -m "docs: GitHub Pages deploy and data refresh guide"
```

---

## 완료 기준 (Phase 1)

- `python -m unittest discover -s tests -p "test_*.py" -v` 전부 PASS
- `node --test tests/` 전부 PASS
- `python build/build.py` → 244건 `index.html` 생성
- 브라우저에서 "홈키파"/"홈매트" 검색·상세·전체목록 정상(Task 6 Step 8 체크리스트)
- README의 GitHub Pages 절차로 배포 가능

## Phase 2 (별도 계획)

바코드 카메라 스캔, 바코드↔제품 메모(○ 연결 / ✕ 허가없음 / ? 모름), 허가없음 캐시 안전장치(되묻기·검색어/확인일 기록·갱신시 자동 재검증·정정), JSON 내보내기/가져오기, HTTPS(이미 GitHub Pages라 충족). → 별도 plan 문서에서 진행.
